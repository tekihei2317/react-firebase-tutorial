import { Book } from 'services/mangarel/models/book'

type CalendarProps = {
  books: Book[]
  loading?: boolean
}

export const Calendar: React.FC<CalendarProps> = () => {
  return <div>test</div>
}
